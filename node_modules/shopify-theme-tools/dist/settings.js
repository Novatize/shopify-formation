"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.settings = void 0;
const path_1 = __importDefault(require("path"));
const fs_1 = __importDefault(require("fs"));
let hasRead = false;
let _settings = {
    translationServiceUrl: "http://localhost:5000",
    translationServiceApiKey: "",
    supportedLanguages: ["en", "fr"],
    defaultLanguage: "en",
};
function getAppSettings() {
    try {
        const configPath = path_1.default.resolve(process.cwd(), "shopify-theme-tools.config.json");
        const data = fs_1.default.readFileSync(configPath, "utf-8");
        return JSON.parse(data);
    }
    catch (e) {
        return {};
    }
}
exports.settings = (() => {
    if (!hasRead) {
        _settings = {
            ..._settings,
            ...getAppSettings(),
        };
    }
    return _settings;
})();
